# Rule: Always Provide Links

- Always provide direct clickable `file://` links to all modified, created, or relevant files in every response.
- Always provide direct clickable HTTP links (e.g. `http://localhost:8080/`) for local development web servers whenever web pages or assets are updated.
