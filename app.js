const projectsData = [
    {
      id: 'wood-engraving-cnc',
      number: 1,
      title: 'Wood Engraving CNC',
      category: 'Real Projects',
      overview: 'A wood engraving CNC (Computer Numerical Control) machine is a computer-controlled tool used for precision engraving, cutting, and carving of intricate designs in wood. It integrates CAD/ CAM software to create and execute designs with high accuracy, using specialized bits to handle various wood types. Commonly employed in woodworking, furniture making, signage, and art, these machines enhance productivity and ensure consistent results, making them ideal for creating decorative items, personalized gifts, and complex 3D carvings.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p1/hero.jpg',
      renderings: [
        'assets/projects/p1/renderings/render_1.jpg',
        'assets/projects/p1/renderings/render_2.jpg',
        'assets/projects/p1/renderings/render_3.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'crop-harvester',
      number: 3,
      title: 'Crop Harvester',
      category: 'Real Projects',
      overview: 'This is agricultural reaper machine designed for efficient harvesting of crops such as wheat, barley, rice, and similar grains. Equipped with sharp cutting blades at the front, it cuts crops close to the ground and neatly arranges them for easier collection. The machine features durable tires for manoeuvrability across various terrains and a robust handlebar with controls for easy operation. Powered by an engine, it provides a reliable and time-saving alternative to manual harvesting, making it ideal for small to medium-scale farmers.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p3/hero.jpg',
      renderings: [
        'assets/projects/p3/renderings/render_1.jpg',
        'assets/projects/p3/renderings/render_2.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'e-scooter',
      number: 2,
      title: 'E-Scooter',
      category: 'Real Projects',
      overview: 'Electric scooters are eco-friendly vehicles designed for convenient urban commuting, powered by an electric motor and a rechargeable battery. Types include foldable scooters, electric mopeds, and off-road models. They feature safety components like brakes and lights, and offer portability and comfort for short trips, making them popular for their cost-effectiveness and ease of use.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p2/hero.jpg',
      renderings: [
        'assets/projects/p2/renderings/render_1.jpg',
        'assets/projects/p2/renderings/render_2.jpg',
        'assets/projects/p2/renderings/render_3.jpg',
        'assets/projects/p2/renderings/render_4.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'egg-incubator',
      number: 4,
      title: 'Egg Incubator',
      category: 'Real Projects',
      overview: 'An egg incubator is a device that creates a controlled environment to ensure the optimal conditions for the hatching of eggs. It is designed to regulate temperature, humidity, and ventilation, which are crucial for embryo development. Key components include a heating element, a thermostat to maintain a consistent temperature, humidity control system, and fans for air circulation. These incubators also feature automatic egg turners that rotate the eggs at regular intervals to mimic the natural process conducted by a brood ing hen. These machines are essential for poultry farming, bird breeding, and various biological research applications.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p4/hero.jpg',
      renderings: [
        'assets/projects/p4/renderings/render_1.jpg',
        'assets/projects/p4/renderings/render_2.jpg',
        'assets/projects/p4/renderings/render_3.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'interactive-kiosk',
      number: 5,
      title: 'Interactive Kiosk',
      category: 'Real Projects',
      overview: 'A kiosk machine is a self-service device designed to provide information or services to users in a variety of public settings. These machines come equipped with a touch-screen interface, allowing users to interact with them easily. They are used for a wide range of applications, such as ticketing, information browsing, wayfinding, ordering food, and even conducting financial transactions.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p5/hero.jpg',
      renderings: [
        'assets/projects/p5/renderings/render_1.jpg',
        'assets/projects/p5/renderings/render_2.jpg',
        'assets/projects/p5/renderings/render_3.jpg',
        'assets/projects/p5/renderings/render_4.jpg',
        'assets/projects/p5/renderings/render_5.jpg',
        'assets/projects/p5/renderings/render_6.jpg',
        'assets/projects/p5/renderings/render_7.jpg',
        'assets/projects/p5/renderings/render_8.png',
        'assets/projects/p5/renderings/render_9.JPG'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'digital-signage',
      number: 6,
      title: 'Digital Signage',
      category: 'Real Projects',
      overview: 'A digital signage display is a large, high-resolution screen designed for commercial and public use. These displays typically feature 4K UHD resolution (3840 x 2160 pixels) for clear and detailed visuals1. They’re built to operate continuously (24/7) and often include advanced technologies like high brightness, dynamic colour range, and anti-glare coating to ensure visibility even in well-lit environments2. The ultra-slim design and various connectivity options, such as HDMI and USB, make them versatile for different settings like retail, corporate offices, and public spaces1. Moreover, they often come with integrated content management systems, enhancing user convenience and efficiency',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p6/hero.jpg',
      renderings: [
        'assets/projects/p6/renderings/render_1.jpg',
        'assets/projects/p6/renderings/render_2.jpg',
        'assets/projects/p6/renderings/render_2.png',
        'assets/projects/p6/renderings/render_3.jpg',
        'assets/projects/p6/renderings/render_3.png',
        'assets/projects/p6/renderings/render_4.jpg',
        'assets/projects/p6/renderings/render_5.jpg'
      ],
      drawings: [
        'assets/projects/p6/drawings/drawing_1.JPG',
        'assets/projects/p6/drawings/drawing_1.pdf',
        'assets/projects/p6/drawings/drawing_2.png',
        'assets/projects/p6/drawings/drawing_3.jpg',
        'assets/projects/p6/drawings/drawing_3.png',
        'assets/projects/p6/drawings/drawing_4.png',
        'assets/projects/p6/drawings/drawing_5.png',
        'assets/projects/p6/drawings/drawing_6.png',
        'assets/projects/p6/drawings/drawing_7.png'
      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'arcade-cabinet',
      number: 7,
      title: 'Arcade Cabinet',
      category: 'Real Projects',
      overview: 'The arcade gaming machine features a high-resolution display screen and a set of responsive controls, including joysticks and buttons, for an engaging and interactive gaming experience. The design is user-friendly and lacks a coin system, ensuring seamless playability. Internally, it’s powered by robust hardware capable of running a wide variety of classic and modern video games. This machine is ideal for various entertainment venues, offering a nostalgic yet contemporary experience for gamers of all ages',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p7/hero.png',
      renderings: [
        'assets/projects/p7/renderings/render_1.jpg',
        'assets/projects/p7/renderings/render_2.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'charcoal-stove',
      number: 8,
      title: 'Charcoal Stove',
      category: 'Real Projects',
      overview: 'I designed this charcoal stove using sheet metal, focusing on maximizing fuel efficiency. It features a dual-air-inlet system (primary and secondary) to ensure complete combustion. By providing secondary air, the stove burns off gases that would otherwise escape as smoke, resulting in a cleaner, hotter flame and less wasted fuel.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p8/hero.jpg',
      renderings: [
        'assets/projects/p8/renderings/render_1.jpg',
        'assets/projects/p8/renderings/render_2.jpg',
        'assets/projects/p8/renderings/render_3.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'distribution-box',
      number: 9,
      title: 'Distribution Box',
      category: 'Real Projects',
      overview: 'This distribution box is a precise replication of a commercial-standard model, designed to ensure that all mechanical clearances and mounting points match the original. It features a recessed housing and a hinged front panel that provides a secure, debris-resistant environment for electrical components. By mapping every dimension exactly, this version maintains the structural quality and component fit of the original while allowing for independent production.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p9/hero.jpg',
      renderings: [
        'assets/projects/p9/renderings/render_1.jpg',
        'assets/projects/p9/renderings/render_2.jpg',
        'assets/projects/p9/renderings/render_3.png'
      ],
      drawings: [
        'assets/projects/p9/drawings/drawing_1.jpg',
        'assets/projects/p9/drawings/drawing_2.jpg',
        'assets/projects/p9/drawings/drawing_3.jpg',
        'assets/projects/p9/drawings/drawing_4.jpg',
        'assets/projects/p9/drawings/drawing_5.jpg',
        'assets/projects/p9/drawings/drawing_6.jpg'
      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'trash-can-01',
      number: 10,
      title: 'Trash Can 01',
      category: 'Real Projects',
      overview: 'his outdoor trash can is built from durable sheet metal and heavy-duty weldments. Custom-ordered for a hospital site, it features integrated LED strips along the sides for high visibility at night, as well as top lighting to clearly illuminate the disposal openings. I also redesigned the intake area to include an internal guide that funnels waste directly into the inner container for better hygiene and easier maintenance.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p10/hero.jpg',
      renderings: [
        'assets/projects/p10/renderings/render_1.jpg',
        'assets/projects/p10/renderings/render_2.jpg',
        'assets/projects/p10/renderings/render_3.jpg',
        'assets/projects/p10/renderings/render_4.png',
        'assets/projects/p10/renderings/render_5.png'
      ],
      drawings: [
        'assets/projects/p10/drawings/drawing_1.jpg',
        'assets/projects/p10/drawings/drawing_2.PDF',
        'assets/projects/p10/drawings/drawing_3.jpg',
        'assets/projects/p10/drawings/drawing_4.jpg',
        'assets/projects/p10/drawings/drawing_5.jpg',
        'assets/projects/p10/drawings/drawing_6.jpg',
        'assets/projects/p10/drawings/drawing_7.jpg',
        'assets/projects/p10/drawings/drawing_8.jpg'
      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'street-light',
      number: 13,
      title: 'Street Light',
      category: 'Real Projects',
      overview: 'A modern dual-arm smart street light designed using SolidWorks weldment and sheet metal features for structural integrity and ease of manufacturing. The arms support imported high-efficiency light modules for primary illumination, while side-mounted LED strips enhance aesthetics and visibility. A base enclosure is integrated at the bottom of the pole to securely house electrical control components. The design combines functionality, style, and manufacturability, making it ideal for smart urban infrastructure',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p13/hero.jpg',
      renderings: [
        'assets/projects/p13/renderings/render_1.jpg',
        'assets/projects/p13/renderings/render_2.jpg',
        'assets/projects/p13/renderings/render_3.png'
      ],
      drawings: [
        'assets/projects/p13/drawings/drawing_1.jpg',
        'assets/projects/p13/drawings/drawing_2.jpg',
        'assets/projects/p13/drawings/drawing_3.jpg',
        'assets/projects/p13/drawings/drawing_4.jpg',
        'assets/projects/p13/drawings/drawing_5.jpg'
      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: '6u-server-rack',
      number: 14,
      title: '6U Server Rack',
      category: 'Real Projects',
      overview: 'A 6U server rack is a compact, rack-mounted enclosure designed to securely house and organize IT hardware, servers, and networking equipment. The “6U” refers to its vertical space, meaning it can accommodate devices up to six rack units high (1U = 1.75 inches or 44.45 mm). This rack is engineered for efficient space management, cooling, and cable organization, making it an essential component in data canters, offices, and industrial setups.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p14/hero.jpg',
      renderings: [
        'assets/projects/p14/renderings/render_1.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'flower-vase',
      number: 15,
      title: 'Flower Vase',
      category: 'Real Projects',
      overview: 'This modern flower vase is crafted entirely from sheet metal, blending a sleek aesthetic with industrial-strength durability. Designed for both office interiors and outdoor environments',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p15/hero.jpg',
      renderings: [
        'assets/projects/p15/renderings/render_1.jpg',
        'assets/projects/p15/renderings/render_2.png',
        'assets/projects/p15/renderings/render_3.png'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'multi-fold-gate',
      number: 16,
      title: 'Multi-Fold Gate',
      category: 'Real Projects',
      overview: 'A multi-fold gate (also known as a bi-fold or accordion gate) is a gate system where each gate wing is divided into two or more panels connected by hinges. Instead of a single large panel swinging in a wide arc, the panels fold back on themselves as the gate opens, similar to how a folding closet door or an accordion works.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p16/hero.png',
      renderings: [

      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'wheel-dolly',
      number: 17,
      title: 'Wheel Dolly',
      category: 'Real Projects',
      overview: 'This mechanical vehicle positioning dolly is engineered for high-capacity manoeuvring in confined workshop environments. The design utilizes a Lever-operated mechanism to provide a high mechanical advantage, allowing a single operator to lift tires effortlessly. Constructed with a heavy-duty welded steel frame and industrial-grade omnidirectional casters, it ensures structural integrity under heavy static loads while maintaining 360-degree mobility.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p17/hero.jpg',
      renderings: [
        'assets/projects/p17/renderings/render_1.jpg',
        'assets/projects/p17/renderings/render_2.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'roller-skate',
      number: 29,
      title: 'Roller Skate',
      category: 'Real Projects',
      overview: 'thanks',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p29/hero.png',
      renderings: [
        'assets/projects/p29/renderings/render_1.jpg',
        'assets/projects/p29/renderings/render_2.jpg',
        'assets/projects/p29/renderings/render_3.jpg',
        'assets/projects/p29/renderings/render_4.jpg',
        'assets/projects/p29/renderings/render_5.jpg',
        'assets/projects/p29/renderings/render_6.JPG'
      ],
      drawings: [

      ],
      dfmTags: ['Real Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'brush-cutter',
      number: 25,
      title: 'Brush Cutter',
      category: 'Experimental Projects',
      overview: 'brush cutter, also known as a brush saw or clearing saw, is a powerful gardening tool used to cut through thick vegetation, including weeds, grass, brush, and small trees. It’s particularly useful in areas where a regular lawn mower would struggle, like uneven terrain, dense undergrowth, and hard-to reach spots. Brush cutters can also be used for crop harvesting, especially for small-scale farming',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p25/hero.jpg',
      renderings: [
        'assets/projects/p25/renderings/render_1.jpg',
        'assets/projects/p25/renderings/render_2.jpg',
        'assets/projects/p25/renderings/render_3.jpg',
        'assets/projects/p25/renderings/render_4.jpg',
        'assets/projects/p25/renderings/render_5.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Experimental Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'ratchet',
      number: 26,
      title: 'Ratchet',
      category: 'Experimental Projects',
      overview: 'A ratchet is a mechanical tool used to apply torque in one direction while allowing movement in the opposite direction. It is commonly used in conjunction with sockets for tightening or loosening fasteners like bolts and nuts. The ratchet mechanism consists of a gear and pawl system, which allows the handle to rotate continuously in one direction while preventing reverse motion, making it more efficient than traditional wrenches.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p26/hero.jpg',
      renderings: [
        'assets/projects/p26/renderings/render_1.jpg',
        'assets/projects/p26/renderings/render_2.jpg',
        'assets/projects/p26/renderings/render_3.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Experimental Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'laminator',
      number: 27,
      title: 'Laminator',
      category: 'Experimental Projects',
      overview: 'A laminator is a device used to protect documents by encasing them in a thin layer of plastic film. It helps preserve important papers, photos, and documents by providing a durable, water-resistant, and tear-proof layer of protection.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p27/hero.jpg',
      renderings: [
        'assets/projects/p27/renderings/render_1.jpg',
        'assets/projects/p27/renderings/render_2.jpg',
        'assets/projects/p27/renderings/render_3.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Experimental Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'maize-thresher',
      number: 21,
      title: 'Maize Thresher',
      category: 'Educational Projects',
      overview: 'A maize thresher machine is an agricultural device designed to efficiently separate maize kernels from cobs, reducing manual labor and increasing productivity. It operates by feeding harvested maize cobs into a threshing drum, where rotating blades or beaters strip the kernels from the cob. The separated kernels are then collected through an output chute, while the cobs and husks are ejected separately.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p21/hero.jpg',
      renderings: [
        'assets/projects/p21/renderings/render_1.jpg',
        'assets/projects/p21/renderings/render_2.jpg',
        'assets/projects/p21/renderings/render_3.jpg',
        'assets/projects/p21/renderings/render_4.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Educational Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'industrial-dolly',
      number: 23,
      title: 'Industrial Dolly',
      category: 'Educational Projects',
      overview: 'This innovative industrial dolly is designed to bridge the gap between traditional two-wheel dollies and paired heavy-load moving systems. Conventional two-wheel dollies are primarily used for transporting small to medium-sized unit loads, such as refrigerators, while heavier loads require a specialized dolly system used in pairs',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p23/hero.jpg',
      renderings: [
        'assets/projects/p23/renderings/render_1.jpg',
        'assets/projects/p23/renderings/render_2.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Educational Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'two-post-lift',
      number: 19,
      title: 'Two Post Lift',
      category: 'Educational Projects',
      overview: 'he two-post lift consists of two vertical columns connected by a top crossbeam, ensuring structural stability. Each column houses lifting arms that extend and adjust to secure the vehicle at designated lifting points. The machine is powered by an electric motor and a hydraulic lifting system, which raises and lowers the vehicle smoothly. The design provides open access to the undercarriage, making it ideal for tasks such as oil changes, brake repairs, and suspension work.',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p19/hero.jpg',
      renderings: [
        'assets/projects/p19/renderings/render_1.jpg',
        'assets/projects/p19/renderings/render_2.jpg',
        'assets/projects/p19/renderings/render_3.jpg',
        'assets/projects/p19/renderings/render_4.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Educational Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'rock-picker',
      number: 18,
      title: 'Rock Picker',
      category: 'Educational Projects',
      overview: 'The rock picker is an agricultural machine designed to efficiently remove rocks and de bris from fields, improving soil quality and protecting other farming equipment. This machine features a durable steel frame, rotating tines, and a conveyor system that separates rocks from the soil while allowing loose dirt to fall back to the ground. The collect ed stones are stored in a rear hopper for easy disposal',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p18/hero.jpg',
      renderings: [
        'assets/projects/p18/renderings/render_1.jpg',
        'assets/projects/p18/renderings/render_2.jpg',
        'assets/projects/p18/renderings/render_3.jpg'
      ],
      drawings: [

      ],
      dfmTags: ['Educational Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    },
    {
      id: 'filing-cabinet',
      number: 28,
      title: 'Filing Cabinet',
      category: 'Educational Projects',
      overview: 'thanks',
      specs: [
        'SolidWorks 2024 Parametric 3D CAD',
        'Sheet Metal Design & DFM Optimization',
        'Shop-Floor Manufacturing Blueprints'
      ],
      image: 'assets/projects/p28/hero.png',
      renderings: [

      ],
      drawings: [

      ],
      dfmTags: ['Educational Projects', 'SolidWorks CAD', 'DFM Optimization', 'Sheet Metal Design']
    }
];
/* ==========================================================================
   SAMUEL ALEMU | MECHANICAL DESIGN ENGINEER PORTFOLIO LOGIC
   YENGUS DESIGN AND MANUFACTURING | ADDIS ABABA, ETHIOPIA
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {

  const ADMIN_EMAIL = "samuelalemu2127@gmail.com";
  const DEFAULT_ADMIN_PASS = "admin123";

  // Map for fast lookup by project ID
  const projectsMap = {};
  
  function getActiveProjects() {
    const deletedIdsRaw = localStorage.getItem('deleted_portfolio_project_ids');
    const deletedIds = deletedIdsRaw ? JSON.parse(deletedIdsRaw) : [];

    const map = {};
    projectsData.forEach(p => {
      if (!deletedIds.includes(p.id)) {
        map[p.id] = p;
      }
    });

    const savedCustom = localStorage.getItem('custom_portfolio_projects');
    if (savedCustom) {
      try {
        const customArr = JSON.parse(savedCustom);
        customArr.forEach(p => {
          if (!deletedIds.includes(p.id)) {
            map[p.id] = p;
          }
        });
      } catch (e) {
        console.error('Error parsing custom_portfolio_projects', e);
      }
    }
    return Object.values(map);
  }

  let activeProjectsList = getActiveProjects();
  activeProjectsList.forEach(p => { projectsMap[p.id] = p; });

  function isAdminLoggedIn() {
    return localStorage.getItem('adminLoggedIn') === 'true';
  }

  /* ------------------------------------------------------------------------
   * Day / Night Theme Controller (Local Storage Sync & CSS Variables)
   * ------------------------------------------------------------------------ */
  const themeToggleBtn = document.getElementById('theme-toggle-btn');
  const sunIcon = document.querySelector('.theme-icon-sun');
  const moonIcon = document.querySelector('.theme-icon-moon');

  function applyTheme(theme) {
    if (theme === 'dark') {
      document.body.classList.add('dark-mode');
      if (sunIcon) sunIcon.style.display = 'none';
      if (moonIcon) moonIcon.style.display = 'block';
    } else {
      document.body.classList.remove('dark-mode');
      if (sunIcon) sunIcon.style.display = 'block';
      if (moonIcon) moonIcon.style.display = 'none';
    }
    localStorage.setItem('portfolio_theme', theme);
  }

  const savedTheme = localStorage.getItem('portfolio_theme') || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
  applyTheme(savedTheme);

  if (themeToggleBtn) {
    themeToggleBtn.addEventListener('click', () => {
      const isDark = document.body.classList.contains('dark-mode');
      applyTheme(isDark ? 'light' : 'dark');
      showToast(isDark ? 'Switched to Light Mode ☀️' : 'Switched to Dark Mode 🌙');
    });
  }

  /* ------------------------------------------------------------------------
   * 1. Top Logo / Name Click Handler (Scroll to Top Home Page)
   * ------------------------------------------------------------------------ */
  const navBrand = document.querySelector('.nav-brand');
  if (navBrand) {
    navBrand.addEventListener('click', (e) => {
      e.preventDefault();
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
  }

  /* ------------------------------------------------------------------------
   * 2. Render Front-Level Project Cards & Admin Controls
   * ------------------------------------------------------------------------ */
  const projectsGrid = document.getElementById('projects-grid');

  function renderProjectCards() {
    if (!projectsGrid) return;

    activeProjectsList = getActiveProjects();
    activeProjectsList.forEach(p => { projectsMap[p.id] = p; });

    const adminActive = isAdminLoggedIn();

    projectsGrid.innerHTML = activeProjectsList.map(project => {
      let categoryClass = 'badge-real';
      if (project.category === 'Experimental Projects') categoryClass = 'badge-experimental';
      if (project.category === 'Educational Projects') categoryClass = 'badge-educational';

      const hasDrawings = project.drawings && project.drawings.length > 0;

      return `
        <div class="project-card card" data-category="${project.category}" data-project="${project.id}" id="card-${project.id}">
          
          ${adminActive ? `
          <!-- Admin On-Card Control Bar -->
          <div style="background: #0f172a; color: #38bdf8; padding: 0.4rem 0.75rem; display: flex; align-items: center; justify-content: space-between; font-size: 0.75rem; font-weight: 700; border-bottom: 1px solid #1e293b;">
            <span>ADMIN CONTROLS</span>
            <div style="display: flex; gap: 0.4rem;">
              <button type="button" class="btn-admin-edit" data-id="${project.id}" style="background: #0284c7; color: #fff; border: none; padding: 0.2rem 0.5rem; border-radius: 4px; cursor: pointer; font-size: 0.75rem; font-weight: 600;">✏️ Edit</button>
              <button type="button" class="btn-admin-delete" data-id="${project.id}" style="background: #ef4444; color: #fff; border: none; padding: 0.2rem 0.5rem; border-radius: 4px; cursor: pointer; font-size: 0.75rem; font-weight: 600;">🗑️ Delete</button>
            </div>
          </div>
          ` : ''}

          <!-- Front-Level Hero Image Container -->
          <div class="project-img-wrapper" style="display: flex; align-items: center; justify-content: center; padding: 0.5rem; min-height: 360px; max-height: 440px; overflow: hidden; position: relative; cursor: pointer;" title="Click hero image to open gallery" data-modal-type="renderings" data-project="${project.id}">
            <img src="${project.image}" alt="${project.title}" class="project-card-img" style="object-fit: contain; max-height: 400px; width: 100%; height: 100%; transition: transform 0.3s ease;" loading="lazy">
            <span class="project-category-badge ${categoryClass}">${project.category}</span>
          </div>

          <!-- Front-Level Card Body -->
          <div class="project-body" style="padding: 1.25rem;">
            <h3 class="project-title" style="font-size: 1.25rem; font-weight: 700; color: var(--text-main); margin-bottom: 1rem;">${project.title}</h3>

            <!-- Action Buttons at Bottom -->
            <div class="card-btn-bar" style="display: flex; gap: 0.45rem; flex-wrap: wrap;">
              
              <button type="button" class="btn btn-outline btn-sm card-tab-btn" data-modal-type="description" data-project="${project.id}">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
                <span>Description</span>
              </button>

              <button type="button" class="btn btn-outline btn-sm card-tab-btn" data-modal-type="specs" data-project="${project.id}">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l-.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                <span>Technical Spec</span>
              </button>

              ${hasDrawings ? `
              <button type="button" class="btn btn-outline btn-sm card-tab-btn" data-modal-type="attachments" data-project="${project.id}">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48"/></svg>
                <span>Attachments (${project.drawings.length})</span>
              </button>` : ''}

            </div>

          </div>

        </div>
      `;
    }).join('');

    attachModalWindowListeners();
    attachAdminCardListeners();
  }

  /* ------------------------------------------------------------------------
   * 3. Dedicated Clean White Modal Windows (Black Text, Scroll Lock, Backdrop Click Exit)
   * ------------------------------------------------------------------------ */
  const itemModal = document.getElementById('item-modal');
  const itemModalCategory = document.getElementById('item-modal-category');
  const itemModalTitle = document.getElementById('item-modal-title');
  const itemModalBody = document.getElementById('item-modal-body');
  const itemModalCounter = document.getElementById('item-modal-counter');
  const itemModalClose = document.getElementById('item-modal-close');
  const itemModalCloseBtn = document.getElementById('item-modal-close-btn');

  let currentProject = null;
  let currentRenderIndex = 0;

  function openModalWindow() {
    if (!itemModal) return;
    document.body.style.overflow = 'hidden'; // Lock background page scroll!
    if (typeof itemModal.showModal === 'function') {
      itemModal.showModal();
    } else {
      itemModal.setAttribute('open', '');
    }
  }

  function closeItemModal() {
    if (itemModal) {
      document.body.style.overflow = ''; // Unlock background page scroll!
      if (typeof itemModal.close === 'function') {
        itemModal.close();
      } else {
        itemModal.removeAttribute('open');
      }
    }
  }

  if (itemModalClose) itemModalClose.addEventListener('click', closeItemModal);
  if (itemModalCloseBtn) itemModalCloseBtn.addEventListener('click', closeItemModal);
  
  if (itemModal) {
    // Close modal when pressing Escape key
    itemModal.addEventListener('cancel', () => {
      document.body.style.overflow = '';
    });

    // Close modal when clicking OUTSIDE the modal content window (on the backdrop overlay)
    itemModal.addEventListener('click', (e) => {
      const wrapper = itemModal.querySelector('.modal-wrapper');
      if (wrapper) {
        const rect = wrapper.getBoundingClientRect();
        const isInDialog = (
          e.clientX >= rect.left &&
          e.clientX <= rect.right &&
          e.clientY >= rect.top &&
          e.clientY <= rect.bottom
        );
        if (!isInDialog) {
          closeItemModal();
        }
      }
    });
  }

  function attachModalWindowListeners() {
    document.querySelectorAll('[data-modal-type]').forEach(elem => {
      elem.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();

        const projectId = elem.getAttribute('data-project');
        const modalType = elem.getAttribute('data-modal-type');
        const project = projectsMap[projectId];

        if (!project || !itemModal) return;
        currentProject = project;

        if (itemModalCategory) itemModalCategory.textContent = project.category;
        if (itemModalTitle) itemModalTitle.textContent = project.title;
        if (itemModalCounter) itemModalCounter.textContent = '';

        if (modalType === 'description') {
          itemModalBody.innerHTML = `
            <div style="background: #ffffff; padding: 0.5rem; color: #0f172a;">
              <h3 style="font-size: 1.15rem; font-weight: 700; color: #0284c7; margin-bottom: 0.75rem;">Machine Engineering Overview</h3>
              <p style="font-size: 1.05rem; line-height: 1.7; color: #1e293b; margin: 0;">${project.overview}</p>
            </div>
          `;
        } else if (modalType === 'specs') {
          const specsListHTML = project.specs.map(spec => `<li style="padding: 0.4rem 0; font-size: 0.95rem; color: #1e293b;">• ${spec}</li>`).join('');
          const tagsHTML = project.dfmTags.map(tag => `<span class="tag" style="font-size: 0.8rem; background: #e0f2fe; color: #0369a1; border: 1px solid #bae6fd;">${tag}</span>`).join('');

          itemModalBody.innerHTML = `
            <div style="background: #ffffff; color: #0f172a; display: flex; flex-direction: column; gap: 1.5rem; padding: 0.5rem;">
              <div>
                <h4 style="font-size: 1rem; font-weight: 700; color: #0284c7; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.75rem;">Software & Modeling Tools</h4>
                <ul style="list-style: none; padding-left: 0; margin: 0; color: #1e293b;">
                  <li style="font-size: 0.95rem; padding: 0.3rem 0;">• <strong>SolidWorks 2024 Parametric 3D CAD</strong> — Full part & assembly modeling</li>
                  <li style="font-size: 0.95rem; padding: 0.3rem 0;">• <strong>Sheet Metal Module</strong> — K-Factor bend tables & automated DXF flat patterns</li>
                  <li style="font-size: 0.95rem; padding: 0.3rem 0;">• <strong>Weldments & Structural Tubing</strong> — Frame weldment cut lists & DFM/DFA</li>
                </ul>
              </div>

              <div>
                <h4 style="font-size: 1rem; font-weight: 700; color: #2563eb; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.75rem;">Key Technical Specifications</h4>
                <ul style="list-style: none; padding-left: 0; margin: 0;">
                  ${specsListHTML}
                </ul>
              </div>

              <div>
                <h4 style="font-size: 0.95rem; font-weight: 700; color: #0f172a; margin-bottom: 0.75rem;">DFM Optimization Badges</h4>
                <div class="dfm-tags" style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
                  ${tagsHTML}
                </div>
              </div>
            </div>
          `;
        } else if (modalType === 'renderings') {
          // Build gallery with HERO IMAGE ALWAYS AS FIRST IMAGE (#1)
          let allImages = [project.image];
          if (project.renderings && project.renderings.length > 0) {
            project.renderings.forEach(img => {
              if (img !== project.image && !allImages.includes(img)) {
                allImages.push(img);
              }
            });
          }
          currentProject.allGalleryImages = allImages;
          currentRenderIndex = 0; // Starts at index 0 (Hero Image!)
          renderLightboxView();
        } else if (modalType === 'attachments') {
          if (!project.drawings || project.drawings.length === 0) return;
          const itemsHTML = project.drawings.map((docSrc, idx) => `
            <div style="padding: 0.75rem; text-align: center; background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px;">
              <img src="${docSrc}" alt="Drawing ${idx + 1}" style="max-height: 200px; width: 100%; object-fit: contain;">
              <div style="margin-top: 0.5rem; display: flex; justify-content: space-between; align-items: center;">
                <span style="font-size: 0.85rem; color: #64748b;">Drawing #${idx + 1}</span>
                <a href="${docSrc}" target="_blank" class="btn btn-primary btn-sm" style="padding: 0.25rem 0.75rem; font-size: 0.8rem;">Open File</a>
              </div>
            </div>
          `).join('');

          itemModalBody.innerHTML = `
            <div style="background: #ffffff; color: #0f172a; padding: 0.5rem;">
              <h4 style="font-size: 1.1rem; font-weight: 700; color: #0f172a; margin-bottom: 0.5rem;">Drawings & Attachments (${project.drawings.length})</h4>
              <p style="font-size: 0.9rem; color: #64748b; margin-bottom: 1.25rem;">Technical blueprints, PDF drawings, and manual documentation for this machine.</p>
              <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 1.25rem;">
                ${itemsHTML}
              </div>
            </div>
          `;
        }

        openModalWindow();
      });
    });
  }

  /* ------------------------------------------------------------------------
   * 4. Lightbox Renderer with Hero Image First & High-Contrast Blue Arrows
   * ------------------------------------------------------------------------ */
  function renderLightboxView() {
    const gallery = (currentProject && currentProject.allGalleryImages) ? currentProject.allGalleryImages : [currentProject.image];
    const total = gallery.length;

    if (currentRenderIndex < 0) currentRenderIndex = total - 1;
    if (currentRenderIndex >= total) currentRenderIndex = 0;

    const activeSrc = gallery[currentRenderIndex];

    if (itemModalCounter) {
      itemModalCounter.textContent = `Image ${currentRenderIndex + 1} of ${total} ${currentRenderIndex === 0 ? '(Hero Image)' : ''}`;
    }

    const thumbsHTML = gallery.map((imgSrc, idx) => {
      const isActive = idx === currentRenderIndex;
      return `
        <div class="thumb-wrapper ${isActive ? 'active' : ''}" onclick="window.selectRenderIndex(${idx})" title="View Rendering #${idx + 1}">
          <div class="thumb-spinner" id="thumb-spin-${idx}"></div>
          <img src="${imgSrc}" 
               alt="Thumbnail ${idx + 1}" 
               class="thumb-img" 
               onload="document.getElementById('thumb-spin-${idx}') && (document.getElementById('thumb-spin-${idx}').style.display='none')" 
               onerror="this.style.display='none'; document.getElementById('thumb-spin-${idx}') && (document.getElementById('thumb-spin-${idx}').style.display='none'); document.getElementById('thumb-fallback-${idx}') && (document.getElementById('thumb-fallback-${idx}').style.display='flex')">
          <div class="thumb-fallback" id="thumb-fallback-${idx}" style="display: none;">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#64748b" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
            <span style="font-size: 0.65rem; color: #64748b; font-weight: 600; margin-top: 2px;">Render ${idx + 1}</span>
          </div>
        </div>
      `;
    }).join('');

    itemModalBody.innerHTML = `
      <div style="display: flex; flex-direction: column; background: #ffffff; padding-bottom: 75px;">
        
        <!-- Large Lightbox Viewer (Hero Image First, High-Contrast Blue Next/Prev Arrows) -->
        <div style="position: relative; width: 100%; background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 12px; padding: 1rem; display: flex; align-items: center; justify-content: center; min-height: 440px; max-height: 560px; overflow: hidden;" class="skeleton-shimmer">
          
          <!-- Image Loading Spinner Overlay -->
          <div class="img-loading-spinner" id="modal-img-spinner">
            <div class="spinner-ring"></div>
          </div>

          <!-- Left Previous Arrow Button -->
          <button type="button" onclick="window.navRender(-1)" aria-label="Previous Image" style="position: absolute; left: 14px; top: 50%; transform: translateY(-50%); background: #2563eb; color: #ffffff; border: none; width: 48px; height: 48px; border-radius: 50%; cursor: pointer; user-select: none; -webkit-user-select: none; display: flex; align-items: center; justify-content: center; transition: all 0.2s ease; z-index: 20; box-shadow: 0 4px 14px rgba(37, 99, 235, 0.4);" onmouseover="this.style.background='#1d4ed8'" onmouseout="this.style.background='#2563eb'">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
          </button>

          <!-- Large Main Image -->
          <img src="${activeSrc}" alt="${currentProject.title} Image ${currentRenderIndex + 1}" style="max-height: 520px; width: 100%; height: 100%; object-fit: contain; user-select: none; -webkit-user-select: none; transition: opacity 0.25s ease;" onload="document.getElementById('modal-img-spinner') && (document.getElementById('modal-img-spinner').style.display='none')" onerror="document.getElementById('modal-img-spinner') && (document.getElementById('modal-img-spinner').style.display='none')">

          <!-- Right Next Arrow Button -->
          <button type="button" onclick="window.navRender(1)" aria-label="Next Image" style="position: absolute; right: 14px; top: 50%; transform: translateY(-50%); background: #2563eb; color: #ffffff; border: none; width: 48px; height: 48px; border-radius: 50%; cursor: pointer; user-select: none; -webkit-user-select: none; display: flex; align-items: center; justify-content: center; transition: all 0.2s ease; z-index: 20; box-shadow: 0 4px 14px rgba(37, 99, 235, 0.4);" onmouseover="this.style.background='#1d4ed8'" onmouseout="this.style.background='#2563eb'">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
          </button>
        </div>

        <!-- Fixed Bottom Thumbnail Strip (Statically Docked at Viewport Bottom) -->
        <div class="docked-thumb-strip">
          ${thumbsHTML}
        </div>
      </div>
    `;
  }

  window.navRender = function(direction) {
    currentRenderIndex += direction;
    renderLightboxView();
  };

  window.selectRenderIndex = function(idx) {
    currentRenderIndex = idx;
    renderLightboxView();
  };

  document.addEventListener('keydown', (e) => {
    if (itemModal && itemModal.hasAttribute('open')) {
      if (e.key === 'ArrowLeft') {
        window.navRender(-1);
      } else if (e.key === 'ArrowRight') {
        window.navRender(1);
      }
    }
  });

  // Initial render
  renderProjectCards();

  /* ------------------------------------------------------------------------
   * 5. Mobile Menu Toggle
   * ------------------------------------------------------------------------ */
  const menuToggle = document.getElementById('menu-toggle');
  const navMenu = document.getElementById('nav-menu');

  if (menuToggle && navMenu) {
    menuToggle.addEventListener('click', () => {
      navMenu.classList.toggle('active');
      const isExpanded = navMenu.classList.contains('active');
      menuToggle.setAttribute('aria-expanded', isExpanded);
    });

    document.querySelectorAll('.nav-link').forEach(link => {
      link.addEventListener('click', () => {
        navMenu.classList.remove('active');
      });
    });
  }

  /* ------------------------------------------------------------------------
   * 6. Strict Navigation & Tab Content Filtering (Viewport State Isolation)
   * ------------------------------------------------------------------------ */
  const allTabSections = document.querySelectorAll('main section[id]');
  const allNavTabLinks = document.querySelectorAll('.nav-link');

  function activateStrictTab(targetId) {
    if (!targetId || targetId === 'hero' || targetId === 'home' || targetId === 'all') {
      allTabSections.forEach(sec => {
        sec.style.display = '';
        sec.classList.remove('tab-hidden');
      });
      allNavTabLinks.forEach(link => link.classList.remove('active'));
      window.scrollTo({ top: 0, behavior: 'smooth' });
      return;
    }

    // Completely hide non-selected sections!
    allTabSections.forEach(sec => {
      const secId = sec.getAttribute('id');
      if (secId === targetId) {
        sec.style.display = 'block';
        sec.classList.remove('tab-hidden');
      } else {
        sec.style.display = 'none';
        sec.classList.add('tab-hidden');
      }
    });

    // Update active navbar tab link state
    allNavTabLinks.forEach(link => {
      const href = link.getAttribute('href');
      if (href === `#${targetId}`) {
        link.classList.add('active');
      } else {
        link.classList.remove('active');
      }
    });

    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  // Bind top navbar tabs
  allNavTabLinks.forEach(link => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const href = link.getAttribute('href');
      if (href && href.startsWith('#')) {
        const targetId = href.replace('#', '');
        activateStrictTab(targetId);
        history.pushState(null, '', `#${targetId}`);
      }
    });
  });

  // Check initial URL hash for active tab section
  if (window.location.hash && window.location.hash !== '#admin') {
    const initId = window.location.hash.replace('#', '');
    if (document.getElementById(initId)) {
      activateStrictTab(initId);
    }
  }

  /* ------------------------------------------------------------------------
   * 7. Projects Category Filter (All, Real Projects, Experimental Projects, Educational Projects)
   * ------------------------------------------------------------------------ */
  const filterBtns = document.querySelectorAll('.filter-btn');

  filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      filterBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');

      const filterValue = btn.getAttribute('data-filter');
      const cards = document.querySelectorAll('.project-card');

      cards.forEach(card => {
        const cardCategory = card.getAttribute('data-category');
        if (filterValue === 'all' || filterValue === 'All' || filterValue === cardCategory) {
          card.style.display = 'flex';
          setTimeout(() => {
            card.style.opacity = '1';
            card.style.transform = 'scale(1)';
          }, 50);
        } else {
          card.style.opacity = '0';
          card.style.transform = 'scale(0.95)';
          setTimeout(() => {
            card.style.display = 'none';
          }, 300);
        }
      });
    });
  });

  /* ------------------------------------------------------------------------
   * 8. Interactive Contact Form Validation & Toast Notification
   * ------------------------------------------------------------------------ */
  const contactForm = document.getElementById('contact-form');
  const submitBtn = document.getElementById('submit-btn');

  if (contactForm) {
    contactForm.addEventListener('submit', (e) => {
      e.preventDefault();

      let isValid = true;

      const nameInput = document.getElementById('name');
      const emailInput = document.getElementById('email');
      const messageInput = document.getElementById('message');

      const validateInput = (input, condition) => {
        const group = input.closest('.form-group');
        if (!condition) {
          if (group) group.classList.add('has-error');
          isValid = false;
        } else {
          if (group) group.classList.remove('has-error');
        }
      };

      if (nameInput) validateInput(nameInput, nameInput.value.trim().length > 1);
      
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (emailInput) validateInput(emailInput, emailRegex.test(emailInput.value.trim()));
      
      if (messageInput) validateInput(messageInput, messageInput.value.trim().length > 5);

      if (isValid && submitBtn) {
        const originalText = submitBtn.innerHTML;
        submitBtn.disabled = true;
        submitBtn.innerHTML = `
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="animation: spin 1s linear infinite"><line x1="12" y1="2" x2="12" y2="6"/><line x1="12" y1="18" x2="12" y2="22"/><line x1="4.93" y1="4.93" x2="7.76" y2="7.76"/><line x1="16.24" y1="16.24" x2="19.07" y2="19.07"/><line x1="2" y1="12" x2="6" y2="12"/><line x1="18" y1="12" x2="22" y2="12"/><line x1="4.93" y1="19.07" x2="7.76" y2="16.24"/><line x1="16.24" y1="7.76" x2="19.07" y2="4.93"/></svg>
          <span>Sending Engineering Specs...</span>
        `;

        setTimeout(() => {
          showToast('Message sent successfully! Samuel Alemu will review your mechanical design requirements shortly.');
          contactForm.reset();
          submitBtn.disabled = false;
          submitBtn.innerHTML = originalText;
        }, 1200);
      }
    });
  }

  /* ------------------------------------------------------------------------
   * 9. Toast Notification Helper
   * ------------------------------------------------------------------------ */
  function showToast(message) {
    const toastContainer = document.getElementById('toast-container');
    if (!toastContainer) return;

    const toast = document.createElement('div');
    toast.className = 'toast';
    toast.innerHTML = `
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#0d9488" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
      <span>${message}</span>
    `;

    toastContainer.appendChild(toast);

    setTimeout(() => {
      toast.style.opacity = '0';
      toast.style.transform = 'translateY(10px)';
      toast.style.transition = 'all 0.3s ease';
      setTimeout(() => toast.remove(), 300);
    }, 4000);
  }

  /* ------------------------------------------------------------------------
   * 10. Hidden Admin Portal (/admin) & Direct File Upload System
   * ------------------------------------------------------------------------ */
  const adminTopBar = document.getElementById('admin-top-bar');
  const adminAddProjectBtn = document.getElementById('admin-add-project-btn');
  const adminLogoutBtn = document.getElementById('admin-logout-btn');

  const adminLoginModal = document.getElementById('admin-login-modal');
  const adminLoginForm = document.getElementById('admin-login-form');
  const adminLoginClose = document.getElementById('admin-login-close');
  const adminLoginError = document.getElementById('admin-login-error');
  
  const adminProjectModal = document.getElementById('admin-project-modal');
  const adminProjectForm = document.getElementById('admin-project-form');
  const adminProjectClose = document.getElementById('admin-project-close');
  const adminProjectCancel = document.getElementById('admin-project-cancel');
  const adminProjectModalTitle = document.getElementById('admin-project-modal-title');

  const projectsAdminActions = document.getElementById('projects-admin-actions');
  const btnGoogleLogin = document.getElementById('btn-google-login');

  function checkAdminRouteAndState() {
    const isRouteAdmin = (
      window.location.hash.toLowerCase() === '#admin' ||
      window.location.pathname.toLowerCase().endsWith('/admin') ||
      window.location.pathname.toLowerCase().endsWith('/admin.html') ||
      window.location.search.toLowerCase().includes('admin')
    );

    if (isAdminLoggedIn()) {
      if (projectsAdminActions) projectsAdminActions.style.display = 'flex';
      renderProjectCards();
    } else {
      if (projectsAdminActions) projectsAdminActions.style.display = 'none';
      if (isRouteAdmin && adminLoginModal) {
        if (typeof adminLoginModal.showModal === 'function') adminLoginModal.showModal();
        else adminLoginModal.setAttribute('open', '');
      }
    }
  }

  if (btnGoogleLogin) {
    btnGoogleLogin.addEventListener('click', () => {
      showToast('Authenticating with Google OAuth...');
      setTimeout(() => {
        localStorage.setItem('adminLoggedIn', 'true');
        showToast('Signed in via Google OAuth: samuelalemu2127@gmail.com');
        if (adminLoginModal) {
          if (typeof adminLoginModal.close === 'function') adminLoginModal.close();
          else adminLoginModal.removeAttribute('open');
        }
        checkAdminRouteAndState();
      }, 800);
    });
  }

  checkAdminRouteAndState();
  window.addEventListener('hashchange', checkAdminRouteAndState);

  if (adminAddProjectBtn) {
    adminAddProjectBtn.addEventListener('click', () => {
      openProjectFormModal();
    });
  }

  if (adminLogoutBtn) {
    adminLogoutBtn.addEventListener('click', () => {
      localStorage.removeItem('adminLoggedIn');
      if (projectsAdminActions) projectsAdminActions.style.display = 'none';
      showToast('Logged out of Admin Portal.');

      if (window.location.pathname.toLowerCase().includes('/admin')) {
        window.location.href = '/';
      } else {
        if (window.location.hash === '#admin') {
          history.pushState("", document.title, window.location.pathname + window.location.search);
        }
        renderProjectCards();
      }
    });
  }

  if (adminLoginClose && adminLoginModal) {
    adminLoginClose.addEventListener('click', () => {
      if (typeof adminLoginModal.close === 'function') adminLoginModal.close();
      else adminLoginModal.removeAttribute('open');
    });
  }

  if (adminLoginForm) {
    adminLoginForm.addEventListener('submit', (e) => {
      e.preventDefault();
      const email = document.getElementById('admin-email-input').value.trim();
      const pass = document.getElementById('admin-password-input').value.trim();

      if (email.toLowerCase() === ADMIN_EMAIL.toLowerCase() && pass.length > 0) {
        localStorage.setItem('adminLoggedIn', 'true');
        if (adminLoginError) adminLoginError.style.display = 'none';
        if (typeof adminLoginModal.close === 'function') adminLoginModal.close();
        else adminLoginModal.removeAttribute('open');
        showToast(`Welcome Admin Samuel Alemu (${ADMIN_EMAIL})! Admin mode activated.`);
        checkAdminRouteAndState();
        renderProjectCards();
      } else {
        if (adminLoginError) adminLoginError.style.display = 'block';
      }
    });
  }

  /* ------------------------------------------------------------------------
   * Canvas Image Compression & Async Multi-File Payload Handling
   * ------------------------------------------------------------------------ */
  function compressImage(file, maxWidth = 1200, maxHeight = 1200, quality = 0.75) {
    return new Promise((resolve) => {
      if (!file || !file.type) {
        resolve(null);
        return;
      }

      if (!file.type.startsWith('image/')) {
        const reader = new FileReader();
        reader.onload = (e) => resolve(e.target.result);
        reader.onerror = () => resolve(null);
        reader.readAsDataURL(file);
        return;
      }

      const reader = new FileReader();
      reader.onload = (event) => {
        const img = new Image();
        img.onload = () => {
          let width = img.width;
          let height = img.height;

          if (width > maxWidth || height > maxHeight) {
            if (width / height > maxWidth / maxHeight) {
              height = Math.round((height * maxWidth) / width);
              width = maxWidth;
            } else {
              width = Math.round((width * maxHeight) / height);
              height = maxHeight;
            }
          }

          const canvas = document.createElement('canvas');
          canvas.width = width;
          canvas.height = height;

          const ctx = canvas.getContext('2d');
          ctx.drawImage(img, 0, 0, width, height);

          const compressedDataUrl = canvas.toDataURL('image/jpeg', quality);
          resolve(compressedDataUrl);
        };
        img.onerror = () => resolve(event.target.result);
        img.src = event.target.result;
      };
      reader.onerror = () => resolve(null);
      reader.readAsDataURL(file);
    });
  }

  /* --- Direct File Upload Handlers (Hero Photo, Gallery Renderings, PDF Attachments) --- */
  const btnUploadHero = document.getElementById('btn-upload-hero');
  const projHeroFile = document.getElementById('proj-hero-file');
  const heroPreviewBox = document.getElementById('hero-preview-box');
  const heroPreviewImg = document.getElementById('hero-preview-img');
  const heroPreviewFilename = document.getElementById('hero-preview-filename');

  const btnUploadRenderings = document.getElementById('btn-upload-renderings');
  const projRenderingsFile = document.getElementById('proj-renderings-file');
  const renderingsPreviewBox = document.getElementById('renderings-preview-box');

  const btnUploadDrawings = document.getElementById('btn-upload-drawings');
  const projDrawingsFile = document.getElementById('proj-drawings-file');
  const drawingsPreviewBox = document.getElementById('drawings-preview-box');

  // Hero File Upload Trigger
  if (btnUploadHero && projHeroFile) {
    btnUploadHero.addEventListener('click', () => projHeroFile.click());
    projHeroFile.addEventListener('change', async (e) => {
      const file = e.target.files[0];
      if (file) {
        showToast(`Processing & optimizing "${file.name}"...`);
        try {
          const dataUrl = await compressImage(file, 1200, 1200, 0.8);
          if (dataUrl) {
            document.getElementById('proj-image').value = dataUrl;
            if (heroPreviewImg) heroPreviewImg.src = dataUrl;
            if (heroPreviewFilename) heroPreviewFilename.textContent = file.name;
            if (heroPreviewBox) heroPreviewBox.style.display = 'flex';
            showToast(`Hero cover photo "${file.name}" loaded successfully!`);
          }
        } catch (err) {
          console.error('Error processing hero image:', err);
          showToast('Failed to process image file.', 'error');
        }
      }
    });
  }

  // Renderings Gallery Files Upload Trigger
  if (btnUploadRenderings && projRenderingsFile) {
    btnUploadRenderings.addEventListener('click', () => projRenderingsFile.click());
    projRenderingsFile.addEventListener('change', async (e) => {
      const files = Array.from(e.target.files);
      if (files.length > 0) {
        showToast(`Optimizing ${files.length} gallery photos asynchronously...`);
        try {
          const uploadPromises = files.map(file => compressImage(file, 1200, 1200, 0.75));
          const compressedDataUrls = await Promise.all(uploadPromises);
          const validUrls = compressedDataUrls.filter(Boolean);

          const currentInput = document.getElementById('proj-renderings');
          const existing = currentInput.value.trim() ? currentInput.value.trim().split(',').map(s => s.trim()) : [];
          const combined = existing.concat(validUrls);
          currentInput.value = combined.join(', ');

          renderGalleryPreviews(combined);
          showToast(`${validUrls.length} gallery photos attached & optimized!`);
          projRenderingsFile.value = '';
        } catch (err) {
          console.error('Error processing gallery photos:', err);
          showToast('Failed to process gallery photos.', 'error');
        }
      }
    });
  }

  function renderGalleryPreviews(urlList) {
    if (!renderingsPreviewBox) return;
    renderingsPreviewBox.innerHTML = urlList.map((url, i) => `
      <div style="position: relative; display: inline-block; margin: 4px;" data-preview-index="${i}">
        <img src="${url}" style="height: 70px; width: 90px; object-fit: cover; border-radius: 6px; border: 1px solid #cbd5e1;">
        <button type="button" class="btn-remove-rendering" data-index="${i}" style="position: absolute; top: -6px; right: -6px; background: #ef4444; color: white; border: none; border-radius: 50%; width: 20px; height: 20px; display: flex; align-items: center; justify-content: center; font-size: 12px; font-weight: bold; cursor: pointer; line-height: 1;">×</button>
      </div>
    `).join('');

    renderingsPreviewBox.querySelectorAll('.btn-remove-rendering').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const idx = parseInt(btn.getAttribute('data-index'), 10);
        const currentInput = document.getElementById('proj-renderings');
        let arr = currentInput.value.trim().split(',').map(s => s.trim()).filter(Boolean);
        arr.splice(idx, 1);
        currentInput.value = arr.join(', ');
        renderGalleryPreviews(arr);
      });
    });
  }

  // PDF Drawings / Attachments Upload Trigger
  if (btnUploadDrawings && projDrawingsFile) {
    btnUploadDrawings.addEventListener('click', () => projDrawingsFile.click());
    projDrawingsFile.addEventListener('change', async (e) => {
      const files = Array.from(e.target.files);
      if (files.length > 0) {
        showToast(`Processing ${files.length} attachment files...`);
        try {
          const uploadPromises = files.map(file => compressImage(file, 1600, 1600, 0.8));
          const fileUrls = await Promise.all(uploadPromises);
          const validUrls = fileUrls.filter(Boolean);

          const currentInput = document.getElementById('proj-drawings');
          const existing = currentInput.value.trim() ? currentInput.value.trim().split(',').map(s => s.trim()) : [];
          const combined = existing.concat(validUrls);
          currentInput.value = combined.join(', ');

          renderDrawingPreviews(combined);
          showToast(`${validUrls.length} PDF / drawing attachments added!`);
          projDrawingsFile.value = '';
        } catch (err) {
          console.error('Error processing drawing attachments:', err);
          showToast('Failed to process attachment files.', 'error');
        }
      }
    });
  }

  function renderDrawingPreviews(urlList) {
    if (!drawingsPreviewBox) return;
    drawingsPreviewBox.innerHTML = urlList.map((url, i) => `
      <div style="background: #faf5ff; color: #7e22ce; border: 1px solid #e9d5ff; padding: 0.4rem 0.75rem; border-radius: 6px; font-size: 0.8rem; font-weight: 600; display: inline-flex; align-items: center; gap: 0.5rem; margin: 4px;" data-drawing-index="${i}">
        <span>📄 Attachment ${i + 1}</span>
        <button type="button" class="btn-remove-drawing" data-index="${i}" style="background: #ef4444; color: white; border: none; border-radius: 50%; width: 18px; height: 18px; display: flex; align-items: center; justify-content: center; font-size: 11px; cursor: pointer;">×</button>
      </div>
    `).join('');

    drawingsPreviewBox.querySelectorAll('.btn-remove-drawing').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const idx = parseInt(btn.getAttribute('data-index'), 10);
        const currentInput = document.getElementById('proj-drawings');
        let arr = currentInput.value.trim().split(',').map(s => s.trim()).filter(Boolean);
        arr.splice(idx, 1);
        currentInput.value = arr.join(', ');
        renderDrawingPreviews(arr);
      });
    });
  }

  function resetFormAndPreviews() {
    if (adminProjectForm) adminProjectForm.reset();

    if (projHeroFile) projHeroFile.value = '';
    if (projRenderingsFile) projRenderingsFile.value = '';
    if (projDrawingsFile) projDrawingsFile.value = '';

    document.getElementById('proj-image').value = '';
    document.getElementById('proj-renderings').value = '';
    if (document.getElementById('proj-drawings')) document.getElementById('proj-drawings').value = '';

    if (heroPreviewBox) heroPreviewBox.style.display = 'none';
    if (heroPreviewImg) heroPreviewImg.src = '';
    if (heroPreviewFilename) heroPreviewFilename.textContent = '';
    if (renderingsPreviewBox) renderingsPreviewBox.innerHTML = '';
    if (drawingsPreviewBox) drawingsPreviewBox.innerHTML = '';
  }

  function openProjectFormModal(projectToEdit = null) {
    if (!adminProjectModal) return;
    resetFormAndPreviews();

    if (projectToEdit) {
      if (adminProjectModalTitle) adminProjectModalTitle.textContent = '✏️ Edit Project';
      document.getElementById('admin-edit-project-id').value = projectToEdit.id;
      document.getElementById('proj-title').value = projectToEdit.title || '';
      document.getElementById('proj-category').value = projectToEdit.category || 'Real Projects';
      document.getElementById('proj-overview').value = projectToEdit.overview || '';
      document.getElementById('proj-specs').value = (projectToEdit.specs || []).join('\n');
      document.getElementById('proj-image').value = projectToEdit.image || '';
      document.getElementById('proj-renderings').value = (projectToEdit.renderings || []).join(', ');
      if (document.getElementById('proj-drawings')) document.getElementById('proj-drawings').value = (projectToEdit.drawings || []).join(', ');
      document.getElementById('proj-tags').value = (projectToEdit.dfmTags || []).join(', ');

      if (projectToEdit.image) {
        if (heroPreviewImg) heroPreviewImg.src = projectToEdit.image;
        if (heroPreviewBox) heroPreviewBox.style.display = 'flex';
      }
      if (projectToEdit.renderings && projectToEdit.renderings.length > 0) {
        renderGalleryPreviews(projectToEdit.renderings);
      }
      if (projectToEdit.drawings && projectToEdit.drawings.length > 0) {
        renderDrawingPreviews(projectToEdit.drawings);
      }
    } else {
      if (adminProjectModalTitle) adminProjectModalTitle.textContent = '➕ Add New Project';
      document.getElementById('admin-edit-project-id').value = '';
    }

    if (typeof adminProjectModal.showModal === 'function') adminProjectModal.showModal();
    else adminProjectModal.setAttribute('open', '');
  }

  function closeProjectFormModal() {
    if (adminProjectModal) {
      if (typeof adminProjectModal.close === 'function') adminProjectModal.close();
      else adminProjectModal.removeAttribute('open');
      resetFormAndPreviews();
    }
  }

  if (adminProjectClose) adminProjectClose.addEventListener('click', closeProjectFormModal);
  if (adminProjectCancel) adminProjectCancel.addEventListener('click', closeProjectFormModal);

  function applyCurrentCategoryFilter() {
    const activeBtn = document.querySelector('.filter-btn.active');
    const filterValue = activeBtn ? activeBtn.getAttribute('data-filter') : 'all';
    const cards = document.querySelectorAll('.project-card');

    cards.forEach(card => {
      const cardCategory = card.getAttribute('data-category');
      if (filterValue === 'all' || filterValue === 'All' || filterValue === cardCategory) {
        card.style.display = 'flex';
        card.style.opacity = '1';
        card.style.transform = 'scale(1)';
      } else {
        card.style.display = 'none';
      }
    });
  }

  if (adminProjectForm) {
    adminProjectForm.addEventListener('submit', (e) => {
      e.preventDefault();

      try {
        const editId = document.getElementById('admin-edit-project-id').value;
        const title = document.getElementById('proj-title').value.trim();
        const category = document.getElementById('proj-category').value;
        const overview = document.getElementById('proj-overview').value.trim();
        const specsRaw = document.getElementById('proj-specs').value.trim();
        const image = document.getElementById('proj-image').value.trim();
        const renderingsRaw = document.getElementById('proj-renderings').value.trim();
        const drawingsRaw = document.getElementById('proj-drawings') ? document.getElementById('proj-drawings').value.trim() : '';
        const tagsRaw = document.getElementById('proj-tags').value.trim();

        if (!title || !overview) {
          showToast('Please fill in required fields: Project Title and Overview.', 'error');
          return;
        }

        const specs = specsRaw ? specsRaw.split('\n').map(s => s.trim()).filter(Boolean) : ['SolidWorks 2024 Parametric 3D CAD', 'Sheet Metal Design & DFM Optimization'];
        const renderings = renderingsRaw ? renderingsRaw.split(',').map(r => r.trim()).filter(Boolean) : (image ? [image] : []);
        const drawings = drawingsRaw ? drawingsRaw.split(',').map(d => d.trim()).filter(Boolean) : [];
        const dfmTags = tagsRaw ? tagsRaw.split(',').map(t => t.trim()).filter(Boolean) : [category, 'SolidWorks CAD', 'DFM Optimization'];

        // Dynamic unique ID preventing React/DOM state key collisions on subsequent additions
        const projId = editId || `proj-${Date.now()}-${Math.floor(Math.random() * 10000)}`;

        const newProjObj = {
          id: projId,
          number: activeProjectsList.length + 1,
          title,
          category,
          overview,
          specs,
          image: image || (renderings[0] || 'assets/projects/p1/hero.jpg'),
          renderings: renderings.length > 0 ? renderings : [image || 'assets/projects/p1/hero.jpg'],
          drawings,
          dfmTags
        };

        const savedCustom = localStorage.getItem('custom_portfolio_projects');
        let customArr = savedCustom ? JSON.parse(savedCustom) : [];

        if (editId) {
          customArr = customArr.filter(p => p.id !== editId);
        }
        customArr.push(newProjObj);

        // Safe storage save with quota overflow protection
        try {
          localStorage.setItem('custom_portfolio_projects', JSON.stringify(customArr));
        } catch (quotaErr) {
          console.warn('Storage quota limit reached:', quotaErr);
          showToast('Storage quota reached. Saving project with compressed media.', 'warning');
          // Fallback truncation for heavy base64 strings if quota hit
          newProjObj.renderings = newProjObj.renderings.slice(0, 2);
          customArr[customArr.length - 1] = newProjObj;
          try {
            localStorage.setItem('custom_portfolio_projects', JSON.stringify(customArr));
          } catch (e2) {
            console.error('Failed fallback storage write:', e2);
          }
        }

        projectsMap[newProjObj.id] = newProjObj;

        resetFormAndPreviews();
        closeProjectFormModal();

        showToast(editId ? `Project "${title}" updated successfully!` : `New Project "${title}" published live!`);

        // Instant UI synchronization without requiring page refresh
        renderProjectCards();
        applyCurrentCategoryFilter();

      } catch (err) {
        console.error('Error submitting project form:', err);
        showToast(`Failed to create project: ${err.message}`, 'error');
      }
    });
  }

  function attachAdminCardListeners() {
    if (!isAdminLoggedIn()) return;

    document.querySelectorAll('.btn-admin-edit').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const id = btn.getAttribute('data-id');
        const proj = projectsMap[id];
        if (proj) openProjectFormModal(proj);
      });
    });

    document.querySelectorAll('.btn-admin-delete').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const id = btn.getAttribute('data-id');
        const proj = projectsMap[id];
        if (proj && confirm(`Are you sure you want to delete project "${proj.title}"?`)) {
          const savedCustom = localStorage.getItem('custom_portfolio_projects');
          let customArr = savedCustom ? JSON.parse(savedCustom) : [];
          customArr = customArr.filter(p => p.id !== id);
          localStorage.setItem('custom_portfolio_projects', JSON.stringify(customArr));

          const deletedIdsRaw = localStorage.getItem('deleted_portfolio_project_ids');
          let deletedIds = deletedIdsRaw ? JSON.parse(deletedIdsRaw) : [];
          if (!deletedIds.includes(id)) {
            deletedIds.push(id);
          }
          localStorage.setItem('deleted_portfolio_project_ids', JSON.stringify(deletedIds));

          delete projectsMap[id];
          showToast(`Project "${proj.title}" removed permanently.`);
          renderProjectCards();
          applyCurrentCategoryFilter();
        }
      });
    });
  }

});
